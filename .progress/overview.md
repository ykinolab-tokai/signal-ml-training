# Progress Overview

Last updated: 2026-04-24
Source records: `.progress/records.log`

## At a glance
- Overall: active
- Completion: 100% (done=33 / total=33)
- Current risk: The new template is in place, but later session-specific edits could still drift back toward transcription-heavy exercises if comparison or interpretation prompts are omitted.

## Incomplete tasks
- Ordering: status (`blocked` → `in_progress` → `todo`), then priority (`high` → `medium` → `low`), then due asc, then id asc.
- None.

## Focus now
- Preserve the new five-section structure and keep future exercise revisions split between concrete execution and explicit reasoning.

## Milestone notes
- `M001` completed: progress tracking setup, template creation, 28 handouts, and README alignment.
- `M002` completed: `text/` coverage audit, self-contained PyTorch upgrades, and template/index alignment.
- `M003` completed: exercises coverage mapping and handout integration.
- `M004` completed: self-containment review, active-doc cleanup, and legacy artifact deletion.
- `M005` completed: templates now emphasize artifacts, startup checks, step order, checkpoints, and submission conditions; priority autumn and role-sensitive spring handouts were updated to match.
- `M006` completed: policy docs and handouts now default to `.py` scripts, and media-oriented sessions now instruct students to save figures, wave files, and logs instead of relying on direct display or playback.
- `M007` completed: prototype repair, full-corpus conversion, empty-chapter handling, audit, and integrated build verification are complete.
- `M008` completed: formerly empty signal-processing chapters now contain self-study coverage for discrete-time signals, spectrum analysis, and LTI systems.
  Chapter 05 on integrals was also rewritten so the math prerequisites are no longer interrupted by placeholder headings.
  Chapter 11 on PySoundFile and the common onboarding section of chapter 00 were expanded so audio I/O and post-install startup steps are no longer too thin for self-study.
  Chapter 08 on Matplotlib and chapter 12 on Pillow were rewritten to emphasize save-first workflows, practical preprocessing, and tool choice rather than display-only examples.
- `M009` completed: `materials/`, the semester templates, and the materials index now use `この回の目標`, expose minimum required work more clearly, and point to detailed repo-internal or official references more consistently.
- `M010` completed: all 28 handouts now use `この回の目標`, `ミニ講義`, `演習`, `確認ポイント`, `詰まったときに見る資料`, and every `演習` section is split into `基礎レベル` and `発展レベル`.

## Next actions
- codex / 2026-04-24 / Keep future material updates on the same template and check that each session still contains at least one explicit comparison, interpretation, or design-judgment prompt.
